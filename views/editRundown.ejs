<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link href="/public/output.css" rel="stylesheet" />
</head>
<body class="bg-gray-100 p-6">
  <h1 class="text-3xl font-bold mb-6">Edit Roundown</h1>
  <form method="POST" action="/edit_rundown/<%= rundown.id %>" class="bg-white p-6 rounded shadow max-w-md mx-auto">
    <div class="mb-4">
      <label for="kegiatan" class="block mb-1 font-semibold">Kegiatan</label>
      <input type="text" id="kegiatan" name="kegiatan" value="<%= rundown.kegiatan %>" required class="w-full border border-gray-300 rounded px-3 py-2" />
    </div>
    <div class="mb-4">
      <label for="tempat" class="block mb-1 font-semibold">Tempat</label>
      <input type="text" id="tempat" name="tempat" value="<%= rundown.tempat %>" required class="w-full border border-gray-300 rounded px-3 py-2" />
    </div>
    <div class="mb-4">
      <label for="jam" class="block mb-1 font-semibold">Jam</label>
      <input type="time" id="jam" name="jam" value="<%= rundown.jam.substring(0,5) %>" required class="w-full border border-gray-300 rounded px-3 py-2" />
    </div>
    <div class="mb-4">
      <label for="pembawa_acara" class="block mb-1 font-semibold">Pembawa Acara</label>
      <input type="text" id="pembawa_acara" name="pembawa_acara" value="<%= rundown.pembawa_acara %>" required class="w-full border border-gray-300 rounded px-3 py-2" />
    </div>
    <div class="flex justify-end gap-2">
      <a href="/rundown_list/<%= rundown.event_id %>" class="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400">Batal</a>
      <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Simpan</button>
    </div>
  </form>
</body>
</html>
